---
const { title = "New Habit", streak = 0, weekly = 0, target = 7 } = Astro.props;
const pct = Math.min(100, Math.round((weekly / target) * 100));
---
<li class="p-4 rounded-xl bg-zinc-900/60 ring-1 ring-white/10 flex items-center justify-between">
  <div>
    <h3 class="font-semibold text-white">{title}</h3>
    <p class="text-sm text-zinc-400">{streak}-day streak · {weekly}/{target} this week</p>
  </div>
  <div class="w-40 h-2 rounded bg-zinc-800 overflow-hidden">
    <div class="h-full bg-emerald-500" style={`width:${pct}%`}></div>
  </div>
</li>
